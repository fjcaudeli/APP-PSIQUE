// Define los cuatro valores que Inicio espera recibir desde el servicio.
export interface ResumenDashboard {
  pacientesActivos: number;
  sesionesSemana: number;
  pendientesCobro: number;
  horariosDisponibles: number;
}
